import { UpCounter } from '../interfaces';
export type TryCatchOptions = {
    op: string;
};
export declare class PublicSafety {
    private readonly _errorCounter?;
    private _safeCatchMode;
    constructor(_errorCounter?: UpCounter);
    private _callCo11y;
    tryCatch(fn: () => void, options?: TryCatchOptions): void;
    tryCatchReturn<V>(fn: () => V, noopValue: V, options?: TryCatchOptions): V;
    private _prodSafeCatch;
}
