export declare class FuzzyMapper<T> {
    private readonly _fuzzyKeyRange;
    private readonly _map;
    constructor(_fuzzyKeyRange: number);
    add(key: string, fuzzyKey: number, value: T): void;
    pop(key: string, fuzzyKey: number): T;
    clear(): void;
}
