import type { AbstractModuleId, ModuleCompiled, ModuleEntry, ModuleProvider, ProviderContext } from '@lwrjs/types';
import type { Route } from '../identity.js';
export default class ServerModuleProvider implements ModuleProvider {
    name: string;
    version: string;
    routes: Route[];
    constructor(providerConfig: unknown, { config, runtimeEnvironment: { lwrVersion } }: ProviderContext);
    getModuleEntry({ specifier }: AbstractModuleId): Promise<ModuleEntry | undefined>;
    getModule({ specifier, namespace, name }: AbstractModuleId): Promise<ModuleCompiled | undefined>;
}
//# sourceMappingURL=index.d.ts.map