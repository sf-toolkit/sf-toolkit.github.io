import { PayloadProvider, PayloadProviderArgs, Schema, SchematizedPayload } from '../interfaces';
import type { SchematizedData } from './TypeDefinitions';
declare class PayloadUtility {
    checkInputs(schema: Schema, data: SchematizedData): void;
    getExtraFields(schema: Schema, data: SchematizedData): string[];
    getPayloadFromProvider(provider: PayloadProvider, args?: PayloadProviderArgs): SchematizedPayload;
    private _processValidationResults;
    private _traverseFields;
}
export declare const payloadUtility: Readonly<PayloadUtility>;
export {};
