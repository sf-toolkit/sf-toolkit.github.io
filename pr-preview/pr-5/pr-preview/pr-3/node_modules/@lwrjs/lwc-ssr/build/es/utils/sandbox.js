import runCodeOnVm from './sandbox-vm.js';
import runDebugCode from './ssr-debug.js';
export default async function runCode(codes, context, options) {
    // Run in debug mode
    // TODO: replace env variable with CLI
    if (process.env.SSR_DEBUG) {
        // async but don't bother waiting
        return runDebugCode(codes, context, options);
    }
    return runCodeOnVm(codes, context, options);
}
//# sourceMappingURL=sandbox.js.map