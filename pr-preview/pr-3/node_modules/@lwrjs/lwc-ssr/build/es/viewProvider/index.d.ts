import type { CompiledView, ProviderAppConfig, ProviderContext, PublicModuleBundler, PublicResourceRegistry, RuntimeEnvironment, ViewIdentity, ViewProvider } from '@lwrjs/types';
import type { Route } from '../identity.js';
import BaseViewProvider from '@lwrjs/base-view-provider';
export default class LwcViewProvider extends BaseViewProvider implements ViewProvider {
    name: string;
    moduleBundler: PublicModuleBundler;
    resourceRegistry: PublicResourceRegistry;
    routes: Route[];
    runtimeEnvironment: RuntimeEnvironment;
    config: ProviderAppConfig;
    constructor(_pluginConfig: unknown, providerConfig: ProviderContext);
    initialize(): Promise<void>;
    getView(viewId: ViewIdentity): Promise<CompiledView | undefined>;
}
//# sourceMappingURL=index.d.ts.map