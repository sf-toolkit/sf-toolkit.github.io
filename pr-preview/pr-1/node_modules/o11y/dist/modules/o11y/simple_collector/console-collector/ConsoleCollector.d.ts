import { Environment, LogCollector, LogMeta, Schema } from '../interfaces';
import { SchematizedData } from 'o11y/shared';
export declare class ConsoleCollector implements LogCollector {
    private readonly _simpleFilters;
    private readonly _regexFilters;
    constructor(environment?: Environment);
    collect(schema: Schema, data: SchematizedData, logMeta: LogMeta): void;
    private _shouldLog;
    addFilter(filter: string | RegExp): void;
    removeFilter(filter: string | RegExp): void;
    clearFilters(): void;
    getFilters(): (string | RegExp)[];
    private _log;
    private _logCustom;
}
